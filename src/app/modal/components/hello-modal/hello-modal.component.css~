/* Fondo y estilo del modal */
.modal-header, .modal-footer {
  background-color: #2d3748; /* Gris oscuro para contraste */
  color: #f7fafc; /* Blanco suave para el texto */
  border: none;
  justify-content: center;
  padding: 8px;
  margin-top: 5px;
}

.modal-title {
  font-size: 1.5em;
  font-weight: bold;
  color: #ecc94b; /* Amarillo dorado para destacar */
}

/* Botón de cierre */
.modal-header .close {
  color: #f7fafc;
  font-size: 1.2em;
  opacity: 0.7;
}

.modal-header .close:hover {
  opacity: 1;
  color: #e53e3e; /* Rojo suave para botón de cierre */
}

/* Estilos del formulario */
.modal-body {
  background-color: #f7fafc; /* Fondo claro para contraste */
  padding: 20px;
}

.form-group label {
  color: #2d3748; /* Texto en gris oscuro */
  font-weight: 600;
}

.form-control {
  border-radius: 5px;
  border: 1px solid #cbd5e0; /* Gris suave para bordes */
  padding: 10px;
  color: #2d3748;
  background-color: #edf2f7; /* Fondo gris muy claro */
}

.form-control:focus {
  border-color: #4fd1c5; /* Verde menta en focus */
  box-shadow: 0 0 5px rgba(79, 209, 197, 0.5); /* Sombra verde suave */
  outline: none;
}

/* Botones del modal */
.btn-secondary {
  background-color: #a0aec0; /* Gris medio */
  border: none;
  color: #f7fafc;
  padding: 8px 15px;
  transition: background-color 0.3s ease;
}

.btn-secondary:hover {
  background-color: #718096; /* Gris más oscuro en hover */
}

.btn-primary {
  background-color: #4fd1c5; /* Verde menta */
  border: none;
  color: #1a202c; /* Gris oscuro */
  padding: 8px 15px;
  font-weight: bold;
  transition: background-color 0.3s ease;
}

.btn-primary:hover {
  background-color: #38b2ac; /* Verde más oscuro en hover */
}

/* Alineación y espaciado del modal */
.modal-footer {
  display: flex;
  justify-content: space-between;
  padding: 15px;
}

button:disabled {
  background-color: #e2e8f0;
  color: #a0aec0;
  cursor: not-allowed;
}
